# FOMO Shield — Environment Configuration
# This file contains placeholder values for CI/CD builds.
# Real secrets are injected at build time via --dart-define or set in production.
FINNHUB_API_KEY=ci_placeholder
