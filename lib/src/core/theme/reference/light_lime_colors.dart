import 'package:flutter/material.dart';

// ---------------------------------------------------------------------------
// Light Lime — reference color swatch. PLACEHOLDER, NOT LOCKED — first pass
// only, to be tuned against the Home screen the same way Luxury Gold was
// (background first, then border/text/card). Don't build the rest of the
// app against these values yet; they will move.
// ---------------------------------------------------------------------------

/// Primary background — off-white.
const llCream = Color(0xFFF7F9F1);

/// Cards and blocks.
const llCard = Color(0xFFFFFFFF);

/// Borders and strokes.
const llBorder = Color(0xFFE1E8D4);

/// Main accent / buttons / active icons — lime green.
const llLime = Color(0xFF8BC53F);

/// Secondary accent / gradients — brighter lime.
const llLimeBright = Color(0xFFA6D65C);

/// Headers and accent text.
const llHeaderText = Color(0xFF20241A);

/// Body text.
const llBodyText = Color(0xFF6B7060);

/// Background gradient — light corner (top-right). LOCKED IN (2026-09-01):
/// "Pearl White".
const llGradientLight = Color(0xFFF8F6F1);

/// Background gradient — dark corner (bottom-left). LOCKED IN (2026-09-01):
/// "Pistachio Green".
const llGradientDark = Color(0xFF93C572);
